[pytest]
testpaths = tests
# Sandbox copies of the app live under tests/ as well; they carry their own
# test suites and must not be collected twice.
norecursedirs = healthpit_datamodel healthpit_v2 HealthPitHuawei .build .git
